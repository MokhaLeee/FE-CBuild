#include "global.h"
#include "bmunit.h"

#include "common-chax.h"
#include "skill-system.h"

static bool IsSkillLegend(const u8 sid)
{
    
}

static u8 GetUnitLegendarySkill(struct Unit * unit)
{
    struct SkillList * list = GetUnitSkillList(unit);
}
